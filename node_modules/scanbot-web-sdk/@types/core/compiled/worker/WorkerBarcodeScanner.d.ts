import type { BarcodeScannerConfiguration } from "../BarcodeScannerTypes";
import type { BarcodeScannerResult } from "../BarcodeScannerTypes";
import type { Image } from "../bridge/image";
import { ObjectId } from "../utils";
/**
   This class is used inside the JS Worker and interacts directly with the WASM.
*/
export declare class WorkerBarcodeScanner {
    private getWasmModule;
    private readonly objectMap;
    constructor(getWasmModule: () => any);
    create(configuration: BarcodeScannerConfiguration): Promise<ObjectId<"BarcodeScanner">>;
    run(instanceId: ObjectId<"BarcodeScanner">, image: Image): Promise<{
        result: BarcodeScannerResult;
        originalImage: Image;
    }>;
    clearFrameAccumulationCache(instanceId: ObjectId<"BarcodeScanner">): Promise<void>;
    destroy(instanceId: ObjectId<"BarcodeScanner">): Promise<void>;
    private getWasmComponent;
}

import type { DocumentClassifierConfiguration } from "../DocumentClassifierTypes";
import type { DocumentClassifierResult } from "../DocumentClassifierTypes";
import type { Image } from "../bridge/image";
import { ObjectId } from "../utils";
/**
   This class is used inside the JS Worker and interacts directly with the WASM.
*/
export declare class WorkerDocumentClassifier {
    private getWasmModule;
    private readonly objectMap;
    constructor(getWasmModule: () => any);
    create(configuration: DocumentClassifierConfiguration): Promise<ObjectId<"DocumentClassifier">>;
    run(instanceId: ObjectId<"DocumentClassifier">, image: Image): Promise<{
        result: DocumentClassifierResult;
        originalImage: Image;
    }>;
    destroy(instanceId: ObjectId<"DocumentClassifier">): Promise<void>;
    private getWasmComponent;
}

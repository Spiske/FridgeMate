import type { Image } from "../bridge/image";
import type { TextPatternScannerConfiguration } from "../TextPatternScannerTypes";
import type { TextPatternScannerResult } from "../TextPatternScannerTypes";
import { ObjectId } from "../utils";
/**
   This class is used inside the JS Worker and interacts directly with the WASM.
*/
export declare class WorkerTextPatternScanner {
    private getWasmModule;
    private readonly objectMap;
    constructor(getWasmModule: () => any);
    create(configuration: TextPatternScannerConfiguration): Promise<ObjectId<"TextPatternScanner">>;
    run(instanceId: ObjectId<"TextPatternScanner">, image: Image): Promise<{
        result: TextPatternScannerResult;
        originalImage: Image;
    }>;
    cleanRecognitionQueue(instanceId: ObjectId<"TextPatternScanner">): Promise<void>;
    destroy(instanceId: ObjectId<"TextPatternScanner">): Promise<void>;
    private getWasmComponent;
}

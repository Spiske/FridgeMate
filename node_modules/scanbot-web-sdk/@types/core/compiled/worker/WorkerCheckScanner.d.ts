import type { CheckScannerConfiguration } from "../CheckScannerTypes";
import type { CheckScanningResult } from "../CheckScannerTypes";
import type { Image } from "../bridge/image";
import { ObjectId } from "../utils";
/**
   This class is used inside the JS Worker and interacts directly with the WASM.
*/
export declare class WorkerCheckScanner {
    private getWasmModule;
    private readonly objectMap;
    constructor(getWasmModule: () => any);
    create(configuration: CheckScannerConfiguration): Promise<ObjectId<"CheckScanner">>;
    run(instanceId: ObjectId<"CheckScanner">, image: Image): Promise<{
        result: CheckScanningResult;
        originalImage: Image;
    }>;
    destroy(instanceId: ObjectId<"CheckScanner">): Promise<void>;
    private getWasmComponent;
}

import type { CreditCardScannerConfiguration } from "../CreditCardTypes";
import type { CreditCardScanningResult } from "../CreditCardTypes";
import type { Image } from "../bridge/image";
import { ObjectId } from "../utils";
/**
   This class is used inside the JS Worker and interacts directly with the WASM.
*/
export declare class WorkerCreditCardScanner {
    private getWasmModule;
    private readonly objectMap;
    constructor(getWasmModule: () => any);
    create(configuration: CreditCardScannerConfiguration): Promise<ObjectId<"CreditCardScanner">>;
    run(instanceId: ObjectId<"CreditCardScanner">, image: Image): Promise<{
        result: CreditCardScanningResult;
        originalImage: Image;
    }>;
    clearResult(instanceId: ObjectId<"CreditCardScanner">): Promise<void>;
    destroy(instanceId: ObjectId<"CreditCardScanner">): Promise<void>;
    private getWasmComponent;
}

import type { ImageOrigin } from "../ImageTypes";
import type { ImageRotation } from "../ImageTypes";
import type { Image } from "../bridge/image";
import type { Rectangle } from "../utils";
import { ObjectId } from "../utils";
/**
   This class is used inside the JS Worker and interacts directly with the WASM.
*/
export declare class WorkerImageManipulator {
    private getWasmModule;
    private readonly objectMap;
    constructor(getWasmModule: () => any);
    create(): Promise<ObjectId<"ImageManipulator">>;
    cropRect(instanceId: ObjectId<"ImageManipulator">, image: Image, rect: Rectangle): Promise<{
        result: Image;
        originalImage: Image;
    }>;
    rotate(instanceId: ObjectId<"ImageManipulator">, image: Image, rotation: ImageRotation): Promise<{
        result: Image;
        originalImage: Image;
    }>;
    flip(instanceId: ObjectId<"ImageManipulator">, image: Image, origin: ImageOrigin): Promise<{
        result: Image;
        originalImage: Image;
    }>;
    resize(instanceId: ObjectId<"ImageManipulator">, image: Image, size: number): Promise<{
        result: Image;
        originalImage: Image;
    }>;
    destroy(instanceId: ObjectId<"ImageManipulator">): Promise<void>;
    private getWasmComponent;
}

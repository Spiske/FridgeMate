import type { Image } from "../bridge/image";
import type { MrzParserConfiguration } from "../MrzTypes";
import type { MrzScannerConfiguration } from "../MrzTypes";
import type { MrzScannerResult } from "../MrzTypes";
import { ObjectId } from "../utils";
/**
   This class is used inside the JS Worker and interacts directly with the WASM.
*/
export declare class WorkerMrzScanner {
    private getWasmModule;
    private readonly objectMap;
    constructor(getWasmModule: () => any);
    create(configuration: MrzScannerConfiguration): Promise<ObjectId<"MrzScanner">>;
    run(instanceId: ObjectId<"MrzScanner">, image: Image): Promise<{
        result: MrzScannerResult;
        originalImage: Image;
    }>;
    clearResult(instanceId: ObjectId<"MrzScanner">): Promise<void>;
    destroy(instanceId: ObjectId<"MrzScanner">): Promise<void>;
    private getWasmComponent;
}
/**
   This class is used inside the JS Worker and interacts directly with the WASM.
*/
export declare class WorkerMrzParser {
    private getWasmModule;
    private readonly objectMap;
    constructor(getWasmModule: () => any);
    create(configuration: MrzParserConfiguration): Promise<ObjectId<"MrzParser">>;
    parse(instanceId: ObjectId<"MrzParser">, text: string): Promise<MrzScannerResult>;
    destroy(instanceId: ObjectId<"MrzParser">): Promise<void>;
    private getWasmComponent;
}

import type { Image } from "../bridge/image";
import type { VinScannerConfiguration } from "../VinScannerTypes";
import type { VinScannerResult } from "../VinScannerTypes";
import { ObjectId } from "../utils";
/**
   This class is used inside the JS Worker and interacts directly with the WASM.
*/
export declare class WorkerVinScanner {
    private getWasmModule;
    private readonly objectMap;
    constructor(getWasmModule: () => any);
    create(configuration: VinScannerConfiguration): Promise<ObjectId<"VinScanner">>;
    run(instanceId: ObjectId<"VinScanner">, image: Image): Promise<{
        result: VinScannerResult;
        originalImage: Image;
    }>;
    cleanRecognitionQueue(instanceId: ObjectId<"VinScanner">): Promise<void>;
    destroy(instanceId: ObjectId<"VinScanner">): Promise<void>;
    private getWasmComponent;
}

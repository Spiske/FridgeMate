import { ImageOrigin } from "../ImageTypes";
import { ImageRotation } from "../ImageTypes";
import { Image } from "../bridge/image";
import { Rectangle } from "../utils";
import type { WorkerBridge } from "../../bridge/worker-bridge";
import type { ConsumeType } from "../../bridge/utils";
export type ImageManipulatorCroprectResponse = {
    result: Image;
    originalImage: Image;
};
export type ImageManipulatorRotateResponse = {
    result: Image;
    originalImage: Image;
};
export type ImageManipulatorFlipResponse = {
    result: Image;
    originalImage: Image;
};
export type ImageManipulatorResizeResponse = {
    result: Image;
    originalImage: Image;
};
/**
API for image processing. If applicable on the platform, all the methods return images with the same liveness flag as the input image.
*/
export declare class ImageManipulator {
    private workerBridge;
    private objectId;
    private constructor();
    static create(workerBridge: WorkerBridge): Promise<ImageManipulator>;
    /**
    Crops out the given rectangle from the image.
    @param image The image to crop.
    @param rect The rectangle in normalized coordinates to crop.
    @param consumeImage Controls if the image can still be used after calling this method, or if it will be consumed by the method. See {@link ConsumeType} for more details.
    @returns Cropped image.
    */
    cropRect(image: Image, rect: Rectangle, consumeImage?: ConsumeType): Promise<ImageManipulatorCroprectResponse>;
    /**
    Rotates the given image by the given angle.
    @param image The image to rotate
    @param rotation Rotation to be applied
    @param consumeImage Controls if the image can still be used after calling this method, or if it will be consumed by the method. See {@link ConsumeType} for more details.
    @returns Rotated image.
    */
    rotate(image: Image, rotation: ImageRotation, consumeImage?: ConsumeType): Promise<ImageManipulatorRotateResponse>;
    /**
    Flips the given image horizontally or vertically.
    @param image The image to flip
    @param origin Location of the image origin in the image coordinate system.
    @param consumeImage Controls if the image can still be used after calling this method, or if it will be consumed by the method. See {@link ConsumeType} for more details.
    @returns The result of the filter application.
    */
    flip(image: Image, origin: ImageOrigin, consumeImage?: ConsumeType): Promise<ImageManipulatorFlipResponse>;
    /**
    Resizes the given image to the given size.
    @param image The image to resize
    @param size Size of the longer side of the image.
    @param consumeImage Controls if the image can still be used after calling this method, or if it will be consumed by the method. See {@link ConsumeType} for more details.
    @returns Resized image.
    */
    resize(image: Image, size: number, consumeImage?: ConsumeType): Promise<ImageManipulatorResizeResponse>;
    /**
        Frees up any resources associated with this object.
        No other methods may be called on this object after destroy has been called.
    */
    destroy(): Promise<void>;
}

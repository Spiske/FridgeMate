import type { DeepPartial } from "../utils";
import { DocumentClassifierConfiguration } from "../DocumentClassifierTypes";
import { DocumentClassifierResult } from "../DocumentClassifierTypes";
import { Image } from "../bridge/image";
import type { WorkerBridge } from "../../bridge/worker-bridge";
import type { ConsumeType } from "../../bridge/utils";
export type DocumentClassifierRunResponse = {
    result: DocumentClassifierResult;
    originalImage: Image;
};
/**
API for document classification.

Required licence feature(s): DocumentClassification.
*/
export declare class DocumentClassifier {
    private workerBridge;
    private objectId;
    private constructor();
    static create(workerBridge: WorkerBridge, configuration: DeepPartial<DocumentClassifierConfiguration>): Promise<DocumentClassifier>;
    /**
    Classify the document in the given image.
    @param image The image to classify.
    @param consumeImage Controls if the image can still be used after calling this method, or if it will be consumed by the method. See {@link ConsumeType} for more details.
    @returns The result of document classification.
    */
    run(image: Image, consumeImage?: ConsumeType): Promise<DocumentClassifierRunResponse>;
    /**
        Frees up any resources associated with this object.
        No other methods may be called on this object after destroy has been called.
    */
    destroy(): Promise<void>;
}

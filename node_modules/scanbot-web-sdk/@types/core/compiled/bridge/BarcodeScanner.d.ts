import type { DeepPartial } from "../utils";
import { BarcodeScannerConfiguration } from "../BarcodeScannerTypes";
import { BarcodeScannerResult } from "../BarcodeScannerTypes";
import { Image } from "../bridge/image";
import type { WorkerBridge } from "../../bridge/worker-bridge";
import type { ConsumeType } from "../../bridge/utils";
export type BarcodeScannerRunResponse = {
    result: BarcodeScannerResult;
    originalImage: Image;
};
/**
API for barcode scanning.

Required licence feature(s): BarcodeScanner.
*/
export declare class BarcodeScanner {
    private workerBridge;
    private objectId;
    private constructor();
    static create(workerBridge: WorkerBridge, configuration?: DeepPartial<BarcodeScannerConfiguration>): Promise<BarcodeScanner>;
    /**
    Recognize barcodes in the given image.
    @param image The image to recognize barcodes in.
    @param consumeImage Controls if the image can still be used after calling this method, or if it will be consumed by the method. See {@link ConsumeType} for more details.
    @returns The recognized barcodes.
    */
    run(image: Image, consumeImage?: ConsumeType): Promise<BarcodeScannerRunResponse>;
    /**
    Clear the cache of the barcode frame accumulator.@returns
    */
    clearFrameAccumulationCache(): Promise<void>;
    /**
        Frees up any resources associated with this object.
        No other methods may be called on this object after destroy has been called.
    */
    destroy(): Promise<void>;
}

import type { DeepPartial } from "../utils";
import { DocumentQualityAnalyzerConfiguration } from "../DocumentQualityAnalyzerTypes";
import { DocumentQualityAnalyzerResult } from "../DocumentQualityAnalyzerTypes";
import { Image } from "../bridge/image";
import type { WorkerBridge } from "../../bridge/worker-bridge";
import type { ConsumeType } from "../../bridge/utils";
export type DocumentQualityAnalyzerRunResponse = {
    result: DocumentQualityAnalyzerResult;
    originalImage: Image;
};
/**
API for analyzing document quality.

Required licence feature(s): ImageProcessing.
*/
export declare class DocumentQualityAnalyzer {
    private workerBridge;
    private objectId;
    private constructor();
    static create(workerBridge: WorkerBridge, configuration?: DeepPartial<DocumentQualityAnalyzerConfiguration>): Promise<DocumentQualityAnalyzer>;
    /**
    analyze document quality of the given image.
    @param image The image to analyze.
    @param consumeImage Controls if the image can still be used after calling this method, or if it will be consumed by the method. See {@link ConsumeType} for more details.
    @returns The analyzed document quality.
    */
    run(image: Image, consumeImage?: ConsumeType): Promise<DocumentQualityAnalyzerRunResponse>;
    /**
        Frees up any resources associated with this object.
        No other methods may be called on this object after destroy has been called.
    */
    destroy(): Promise<void>;
}

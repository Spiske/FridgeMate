import type { DeepPartial } from "../utils";
import { CheckScannerConfiguration } from "../CheckScannerTypes";
import { CheckScanningResult } from "../CheckScannerTypes";
import { Image } from "../bridge/image";
import type { WorkerBridge } from "../../bridge/worker-bridge";
import type { ConsumeType } from "../../bridge/utils";
export type CheckScannerRunResponse = {
    result: CheckScanningResult;
    originalImage: Image;
};
/**
API for check scanning.

Required licence feature(s): CheckScanner.
*/
export declare class CheckScanner {
    private workerBridge;
    private objectId;
    private constructor();
    static create(workerBridge: WorkerBridge, configuration?: DeepPartial<CheckScannerConfiguration>): Promise<CheckScanner>;
    /**
    Scan check in the given image.
    @param image The image to scan check in.
    @param consumeImage Controls if the image can still be used after calling this method, or if it will be consumed by the method. See {@link ConsumeType} for more details.
    @returns The check scanning result.
    */
    run(image: Image, consumeImage?: ConsumeType): Promise<CheckScannerRunResponse>;
    /**
        Frees up any resources associated with this object.
        No other methods may be called on this object after destroy has been called.
    */
    destroy(): Promise<void>;
}

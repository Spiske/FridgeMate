import type { DeepPartial } from "../utils";
import { Image } from "../bridge/image";
import { MrzParserConfiguration } from "../MrzTypes";
import { MrzScannerConfiguration } from "../MrzTypes";
import { MrzScannerResult } from "../MrzTypes";
import type { WorkerBridge } from "../../bridge/worker-bridge";
import type { ConsumeType } from "../../bridge/utils";
export type MrzScannerRunResponse = {
    result: MrzScannerResult;
    originalImage: Image;
};
/**
API for machine readable zone scanning.

Required licence feature(s): MrzScanner.
*/
export declare class MrzScanner {
    private workerBridge;
    private objectId;
    private constructor();
    static create(workerBridge: WorkerBridge, configuration?: DeepPartial<MrzScannerConfiguration>): Promise<MrzScanner>;
    /**
    Recognize the MRZ in the given image.
    @param image The image to recognize.
    @param consumeImage Controls if the image can still be used after calling this method, or if it will be consumed by the method. See {@link ConsumeType} for more details.
    @returns The recognition result.
    */
    run(image: Image, consumeImage?: ConsumeType): Promise<MrzScannerRunResponse>;
    /**
    Clears the accumulated results. Should be called after a document has been recognized to the customers
    satisfaction to prevent a poisoned cache.
    @returns
    */
    clearResult(): Promise<void>;
    /**
        Frees up any resources associated with this object.
        No other methods may be called on this object after destroy has been called.
    */
    destroy(): Promise<void>;
}
/**
API for machine readable zone parsing.

Required licence feature(s): MrzScanner.
*/
export declare class MrzParser {
    private workerBridge;
    private objectId;
    private constructor();
    static create(workerBridge: WorkerBridge, configuration?: DeepPartial<MrzParserConfiguration>): Promise<MrzParser>;
    /**
    Parse the MRZ string.
    @param text The MRZ string to parse. Should be formatted with newlines.@returns The recognition result.
    */
    parse(text: string): Promise<MrzScannerResult>;
    /**
        Frees up any resources associated with this object.
        No other methods may be called on this object after destroy has been called.
    */
    destroy(): Promise<void>;
}

import type { DeepPartial } from "../utils";
import { Image } from "../bridge/image";
import { TextPatternScannerConfiguration } from "../TextPatternScannerTypes";
import { TextPatternScannerResult } from "../TextPatternScannerTypes";
import type { WorkerBridge } from "../../bridge/worker-bridge";
import type { ConsumeType } from "../../bridge/utils";
export type TextPatternScannerRunResponse = {
    result: TextPatternScannerResult;
    originalImage: Image;
};
/**
API for text pattern scanning.

Required licence feature(s): TextPatternScanner.
*/
export declare class TextPatternScanner {
    private workerBridge;
    private objectId;
    private constructor();
    static create(workerBridge: WorkerBridge, configuration?: DeepPartial<TextPatternScannerConfiguration>): Promise<TextPatternScanner>;
    /**
    Recognize text line in the given image.
    @param image The image to recognize text line in.
    @param consumeImage Controls if the image can still be used after calling this method, or if it will be consumed by the method. See {@link ConsumeType} for more details.
    @returns The recognized text line.
    */
    run(image: Image, consumeImage?: ConsumeType): Promise<TextPatternScannerRunResponse>;
    /**
    Clean the queue of recognize text lines.@returns
    */
    cleanRecognitionQueue(): Promise<void>;
    /**
        Frees up any resources associated with this object.
        No other methods may be called on this object after destroy has been called.
    */
    destroy(): Promise<void>;
}

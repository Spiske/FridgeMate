import { Image } from "../bridge/image";
import { Page } from "../OcrTypes";
import type { WorkerBridge } from "../../bridge/worker-bridge";
import type { ConsumeType } from "../../bridge/utils";
export type OcrEngineRunResponse = {
    result: Page;
    originalImage: Image;
};
/**
API for optical character recognition.

Required licence feature(s): Ocr.
*/
export declare class OcrEngine {
    private workerBridge;
    private objectId;
    private constructor();
    static create(workerBridge: WorkerBridge): Promise<OcrEngine>;
    /**
    Recognize text in the given image.
    @param image The image to recognize text in.
    @param consumeImage Controls if the image can still be used after calling this method, or if it will be consumed by the method. See {@link ConsumeType} for more details.
    @returns Page recognized in the image.
    */
    run(image: Image, consumeImage?: ConsumeType): Promise<OcrEngineRunResponse>;
    /**
        Frees up any resources associated with this object.
        No other methods may be called on this object after destroy has been called.
    */
    destroy(): Promise<void>;
}

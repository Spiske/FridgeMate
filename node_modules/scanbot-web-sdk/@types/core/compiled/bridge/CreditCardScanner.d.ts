import type { DeepPartial } from "../utils";
import { CreditCardScannerConfiguration } from "../CreditCardTypes";
import { CreditCardScanningResult } from "../CreditCardTypes";
import { Image } from "../bridge/image";
import type { WorkerBridge } from "../../bridge/worker-bridge";
import type { ConsumeType } from "../../bridge/utils";
export type CreditCardScannerRunResponse = {
    result: CreditCardScanningResult;
    originalImage: Image;
};
/**
API for credit card scanning.

Required licence feature(s): CreditCardScanner.
*/
export declare class CreditCardScanner {
    private workerBridge;
    private objectId;
    private constructor();
    static create(workerBridge: WorkerBridge, configuration?: DeepPartial<CreditCardScannerConfiguration>): Promise<CreditCardScanner>;
    /**
    Scan the credit card in the given image.
    @param image The image to scan.
    @param consumeImage Controls if the image can still be used after calling this method, or if it will be consumed by the method. See {@link ConsumeType} for more details.
    @returns The scanning result.
    */
    run(image: Image, consumeImage?: ConsumeType): Promise<CreditCardScannerRunResponse>;
    /**
    Clears the accumulated results. Should be called after a document has been scanned to the customers
    satisfaction to prevent a poisoned cache.
    @returns
    */
    clearResult(): Promise<void>;
    /**
        Frees up any resources associated with this object.
        No other methods may be called on this object after destroy has been called.
    */
    destroy(): Promise<void>;
}
